This lab simulates the bound-buffer problem using EZIPC shared memory and semaphores.
The purpose is to show synchronization and how mutex works to allow each producer and consumer
to enter the critical state one at a time.
